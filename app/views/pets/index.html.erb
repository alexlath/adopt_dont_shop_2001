<main>
  <h1>All Pets</h1>
  <% @pets.each do |pet| %>
    <p><%= image_tag pet.image %></p>
    <h1><%= pet.name %></h1>
    <p><%= "Approximate Age: #{pet.approx_age}" %></p>
    <p><%= "Sex: #{pet.sex}" %></p>
    <p><%= "Shelter: #{pet.shelter.name}" %></p>
  <% end %>
</main>